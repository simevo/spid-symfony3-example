security:

    providers:
        spid_user_provider:
            id: example_user_provider

    firewalls:
        spid:
            pattern: /reserved
            guard:
                provider: spid_user_provider
                authenticators:
                - spid.authenticator
